# 第一、二阶段目标卡

- Outcome：交付一个班主任无需培训即可使用的可运行工作台原型。
- Scope：首页、成绩、座次、学生、值日、待办；桌面/手机响应式；本机数据持久化；同一局域网访问。
- Non-goals：本阶段不录入真实学生信息，不接云数据库，不对公网发布。
- Constraints：中文界面；主要操作 1–2 步完成；不依赖在线资源；不以图片中的重复侧栏或旧式表格样式为视觉目标。
- Done when：核心页面均能切换；待办可完成；座次可两次点击交换；成绩可按学科筛选；刷新后状态保留；构建通过；桌面与手机无明显溢出。
- Verification：`npm run build`、本地 HTTP 检查、桌面/手机浏览器截图与核心交互烟测。
- Risk：浏览器本地存储不适合多人协作或敏感数据；跨不同局域网访问需要第二阶段云部署。

## 第二阶段：15班真实数据与多人同步

- Outcome：授权成员在不同网络打开同一网址，查看并更新同一份15班最新数据。
- Data：学校原始 Excel 的“七年级15班”工作表；仅导入姓名与语文、数学、英语、生物、政治、历史、地理和总评等级。
- Privacy：身份号、学籍号、考号不导入；真实文件和学生数据不进入 GitHub；未登录用户无读取权限。
- Access：邮箱一次性登录；owner/editor 可更新，viewer 只读；RLS、历史版本和乐观并发控制。
- Done when：Supabase 项目与成员账号配置完成；真实 Excel 成功导入；两台不同网络设备可实时看到更新；未授权访问测试失败；GitHub Pages 生产构建通过。
- Current blocker：尚缺云数据库项目配置和首个授权账号邮箱，因此真实数据尚未上传，生产站点仍保持演示模式。

## 第三阶段：教师实测问题修复

- Outcome：连续录入多次考试后，每次记录独立保留，可切换、对比并查看单个学生的纵向变化。
- Scope：考试档案、考试切换、学科筛选、与上一次对比、个人成绩轨迹、旧数据自动迁移、桌面/手机交互优化。
- Non-goals：不把等级制强行伪造成原始分数；不公开真实姓名或成绩；本阶段不替教师制定正式评价权重。
- Constraints：一次导入不覆盖历史；主要查看操作不超过两步；等级制与数值制都能使用；旧浏览器数据和旧云端 payload 可继续打开。
- Done when：连续导入 4 次后仍显示 4 条考试；可切换任意考试和学科；排名展示相对上次的变化；点击学生可看到历次轨迹；刷新后记录保留；云端 payload 包含考试历史。
- Verification：新增数据模型回归测试；真实 Excel 的 4 次独立导入模拟；桌面与 390px 手机浏览器检查；构建与控制台检查。
- Risk：当前线上尚未配置 Supabase，跨网络真实同步仍需云端凭据；等级变化只表达相对档位，不等同于原始分数差。
